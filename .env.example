# DataPulse Environment Configuration
# ====================================
# Copy this file to .env and replace placeholder values with your credentials.
# IMPORTANT: Never commit the .env file with real values!
#
# Setup Instructions:
# 1. cp .env.example .env
# 2. Replace placeholder values below
# 3. Verify .env is in .gitignore (it should be!)
# 4. Never share your .env file publicly

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Google Gemini API Key (required for SQL generation)
# Get your key at: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# =============================================================================
# SECURITY SETTINGS (Production)
# =============================================================================

# JWT Secret Key (auto-generated and persisted if not set)
# For production, set a strong 64+ character secret
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# JWT_SECRET_KEY=your_strong_secret_key_here_at_least_64_characters

# CORS Configuration
# In production, set to comma-separated list of allowed domains
# Example: CORS_ORIGINS=https://myapp.com,https://admin.myapp.com
# Default: localhost only (development)
# CORS_ORIGINS=https://yourdomain.com

# =============================================================================
# OPTIONAL PERFORMANCE SETTINGS
# =============================================================================

# Maximum rows returned per query (default: 1000)
# DATAPULSE_MAX_ROWS=1000

# Cache TTL in seconds (default: 3600 = 1 hour)
# DATAPULSE_CACHE_TTL=3600

# Rate limit: requests per minute per user (default: 30)
# DATAPULSE_RATE_LIMIT=30

# Debug mode - enables detailed error messages (default: false)
# DEBUG=false
